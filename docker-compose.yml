services:
  web:
    build: .
    ports:
      - "8000:8000"
    # Mount the .env file so the API key is available inside the container
    env_file:
      - .env
    volumes:
      # Persist the SQLite database and Django sessions between container restarts
      - ./db.sqlite3:/app/db.sqlite3
